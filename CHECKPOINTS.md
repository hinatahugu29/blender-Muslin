# チェックポイント一覧

Blender を触れないときでも進捗を検証できるように、確認項目を「自動(Blender不要)」と
「手動(Blender必須)」に分けて管理する。

- 自動チェックは変更のたびに実行する。
- Blender で確認したら、下のチェックボックスを `[x]` にして結果を追記してください。

## 手作業で残っているもの

**まずここを見てください。** Blender をヘッドレス起動する
`scripts/blender_selftest.py`(340項目)で大半が自動化されたので、
下の CP-B の一覧は**ほとんどが毎回自動で確認されています**。

```bash
blender --background --factory-startup --python scripts/blender_selftest.py
```

自動化できないのは、**画面に出るものと操作の感触**だけです。

### UI 改修(2026-09-15)で増えた確認項目

ヘッドレスでは仕組みまでしか見られず、見え方は確認できていません。

| 残件 | 何を見るか | なぜ自動で見られないか |
|------|-----------|----------------------|
| 2カラム配置 | 設定パネルのラベルが右揃えになり、N パネルの幅で詰まって見えないか | `use_property_split` が立っていることまでしか確認できない。Blender 純正の N パネルでも使う/使わないが割れており(5枚対10枚)、詰まるようなら戻す |
| Play / Pause の切り替え | 再生中にボタンが Pause になり、押すとその場で止まるか | `context.screen` がヘッドレスでは None |
| 生地のサムネイル | Fabric に7種類の絵が並び、押して選べるか | `icon_id` はヘッドレスでは常に 0(アイコンの割り当てに UI が要る)。画像が 128x128 で読めていることまでは確認済み |
| 組み立て直しの警告 | コライダーを差し替えたとき、赤い箱と Restart ボタンが出るか | 検出とオペレータは自動で確認済み。見え方だけ未確認 |
| ~~型紙と着せた姿勢 (M7)~~ | **済み**(実機で一通り確認、2026-09-22)。 型紙を作る段階で Save Dressed Pose が出て、押すと「着せた姿勢から開始(寸法は型紙)」とグリッドのボタンに変わるか。ボタンで型紙の平らな形に戻るか。N パネルの幅で文言が切れないか | 段階の切り替え・寸法・警告は自動で確認済み。見え方と、実際の服で着せ直す操作感だけ未確認 |
| ~~縫い目の属性化 (M7)~~ | **済み**(実機で一通り確認、2026-09-22)。 縫い目リストの頂点数(例 `13↔13`)と ⇔ ボタンが出るか。統合・細分化の後も縫い線がビューポートに正しく描かれるか。⇔ で縫い線が交差する向きに変わるか | 統合・細分化・削除で縫い目が残ること、旧形式の引き継ぎ、行の描画は自動で確認済み。見え方だけ未確認 |
| ~~着せ付け Dress (M7)~~ | **済み**(実機で一通り確認、2026-09-22)。 Dress を押すと布が動き始め、ビューポートに途中経過が出て、ヘッダに「着せ付け中: N ステップ / 最大速度」が出るか。落ち着くと自動で止まり保存されるか。Enter でその場で確定、Esc で元の形に戻るか。その間タイムラインが進まないか | 同期の経路(落ち着きの判定・保存・中止・上限)は自動で確認済み。モーダルはヘッドレスで動かせない |
| ~~布をつまむ (M7)~~ | **済み**(実機で一通り確認、2026-09-22)。 Adjust(または Dress の最中)に布を左ドラッグすると、つまんだところがマウスに付いてくるか。硬すぎて布が暴れないか(`GRAB_COMPLIANCE = 0`)。離すとまた落ち着いて止まるか。布の外を左クリックしたときに選択が変わって困らないか。中ボタンで視点を回せるか | つまむ制約・レイでの拾い方・離した後の再開は自動で確認済み。マウス操作はヘッドレスで動かせない |
| ~~整えるモード Adjust (M7)~~ | **済み**(実機で一通り確認、2026-09-22)。 着せる前はボタンが押せず、理由(先に Dress で着せてください)がツールチップに出るか。着せた後に押すと、落ち着いても終わらずヘッダに「落ち着きました」と出るか。Enter で保存、Esc で元に戻るか | 終わらないこと・保存・中止・押せない条件は自動で確認済み |
| ~~型紙の確定 Lock Pattern (M7)~~ | **済み**(実機で一通り確認、2026-09-22)。 Pattern パネルに Lock Pattern と「曲げて配置する前に確定する」が出るか。押すと「型紙は確定済み」と鍵のボタン(Unlock)、Restore Pattern に変わるか。シミュレーションの結果が出ている間は押せず、理由がツールチップに出るか | 確定・解除・警告・押せない条件は自動で確認済み。見え方だけ未確認 |
| 型紙から UV (M7) | Pattern パネルの Pattern to UV を押すと UV マップ MuslinPattern ができ、UV エディタでピースが重ならず、上下左右が正しい向きで並ぶか。チェッカーテクスチャを貼って、着せた後も升目がゆがまず全ピースで同じ大きさか | 実寸の比・重ならない・向きは自動で確認済み。見え方だけ未確認 |
| ゴム紐 (M7) | Sewing の下の Elastic パネルで、編集モードで裾やウエストの辺を選んで Add Elastic From Selection を押すと、リストに Length 付きで出るか。走らせて布が寄る(ギャザーになる)か。走らせたまま Length を動かすと、その場で締まったり緩んだりするか | 縮み方・走らせたままの変更・統合での引き継ぎ・削除は自動で確認済み。見え方とギャザーの見た目だけ未確認 |

### 以前からの残件

| 残件 | 内容 |
|------|------|
| 縫い線の描画 | ビューポートに線が出るか、アクティブなシームが強調されるか、リストの ⇔ で向きが反転するか |
| パネルの見た目 | 各サブパネルが読めるか、メッシュ未選択時の案内が出るか |
| 操作の感触 | スクラブの反応、再生の滑らかさ、警告メッセージの分かりやすさ |
| ~~実機の Blender 版~~ | **済み**。Blender 5.2.1 LTS でヘッドレステスト 66項目すべて通過 (2026-09-15) |
| ~~掃過判定の追加ぶん~~ | **済み**。Blender 5.1.1 のヘッドレスで 66項目すべて通過し、`Catch Fast Motion` を実機で直接確認した(既定 True、オン/オフとも発散せず走る、パネルに配線あり、Print Timings も動く) (2026-09-15) |

下の CP-B の各項目は、ヘッドレステストが何を見ているかを人間向けに書いた
ものとして残してあります。自動テストが通っていれば、改めて手で追う必要は
ありません。

---

最終更新: 2026-09-15 / cloth_core v0.3.0 / アドオン v0.5.0 / Blender 5.2.1 LTS で検証
（M1・M2・M3・M6 完了。M5 は生地プリセット・オブジェクト単位設定・曲げ制約の
作り直し・収束加速まで。M4 未着手）

自動テストの規模: cargo test 62 / verify_core.py 106 / blender_selftest.py 340

---

## A. 自動チェック(Blender不要) — 現在すべて通過

### A-1. Rust 物理コアの単体テスト

物理コアは pyo3 に依存しない設計なので、Python 抜きでテストできる。

```bash
cargo test --no-default-features --release --manifest-path rust/cloth_core/Cargo.toml
```

| # | テスト | 検証内容 | 状態 |
|---|--------|----------|------|
| A-1-1 | `free_fall_matches_analytic_solution` | 制約なし頂点が `z = -½gt²` に一致 | ✅ |
| A-1-2 | `pinned_vertex_never_moves` | ピン留め頂点が一切動かない | ✅ |
| A-1-3 | `rigid_distance_constraint_preserves_length` | compliance=0 で長さが 1% 以内に保たれる | ✅ |
| A-1-4 | `hanging_grid_stays_stable` | 11×11 グリッドが発散せず伸び誤差 <5% | ✅ |
| A-1-5 | `seam_closure_pulls_pieces_together` | 縫製制約で2辺が引き寄せられる(M3の核) | ✅ |
| A-1-6 | `floor_collision_stops_cloth` | 布が床面を突き抜けない | ✅ |
| A-1-7 | `wind_effect_scales_with_density` | 同じ風で軽い生地ほど流される | ✅ |
| A-1-8 | `static_drape_is_substep_independent` | 静止形状が substeps に依存しない | ✅ |
| A-1-9 | `cloth_drapes_over_sphere_without_penetration` | 全240フレームで球を貫通しない・接触は発生する | ✅ |
| A-1-10 | `moving_collider_pushes_cloth` | 動くコライダーが布を押し上げる(BVH refit) | ✅ |
| A-1-11 | `self_collision_keeps_cloth_stable` | 自己衝突を入れても破裂しない | ✅ |
| A-1-12 | `self_collision_separates_overlapping_vertices` | 重なった頂点が押し離される | ✅ |
| A-1-13 | `set_pinned_preserves_mass_distribution` | ピン付け外しで質量分布が壊れない | ✅ |
| A-1-14〜20 | `collision::tests::*` | 三角形最近接点・BVHが総当たりと一致・探索半径・refit・空間ハッシュ | ✅ |

上の表は M2 までの分。その後に追加した主なものは次のとおり(全 48項目):

| テスト | 検証内容 |
|--------|----------|
| `bending_compliance_controls_cantilever_droop` | 曲げの compliance が剛性として効く(カンチレバー法) |
| `chebyshev_accelerates_without_changing_material` | 収束加速が硬い生地を硬くし、柔らかい生地は変えない |
| `offset_layers_do_not_pass_through_each_other` | 半セルずらした2枚が素通りしない(頂点-三角形) |
| `self_collision_keeps_layers_apart_when_cloth_piles_up` | 崩落して折り重なった布で層が保たれる |
| `untangle_resolves_initial_overlap_without_launching_cloth` | 開始時の食い込みで布が吹き飛ばない |
| `untangle_does_nothing_when_nothing_overlaps` | 重なっていなければ1つも動かさない(ベイク互換) |
| `friction_does_not_compound_with_contact_count` | 摩擦が接触対の個数に依らない |
| `friction_does_not_depend_on_substeps` | 摩擦が Substeps に依らない |
| `fast_collider_does_not_pass_through_cloth` | 速く動くコライダーが布を素通りしない |
| `closest_point_handles_degenerate_triangles` | 退化した三角形で NaN を返さない |
| `parallel_self_collision_is_deterministic` | 並列化しても結果が決定的 |

### A-2. ビルド済み `.pyd` の Python 検証ハーネス

実際に Blender に入れるバイナリを、Blender の外の Python 3.11+ から叩いて検証する。

```bash
powershell -ExecutionPolicy Bypass -File scripts/verify_core.ps1 -Bench
```

77項目すべて PASS(2026-09-15 時点)。A-1 に加えて以下も確認している:

- `core_version()` が取れる = `.pyd` の更新漏れを検出できる
- 巻き戻し(`set_positions`)後の再計算が**ビット一致**(決定的)
- 不正入力(長さ不一致・範囲外インデックス)でクラッシュせず `ValueError`
- アドオン Python モジュール全ファイルの構文チェック

### A-2-1. シームロジック(M3・bpy非依存)

縫い目の中核ロジックは [addon/muslin/seams.py](addon/muslin/seams.py) に
bpy 非依存で切り出してあるため、Blender 抜きで検証できる:

- 選択エッジ集合を連結成分に分解し、端点から順序付けたチェーンにする
- エッジの入力順に結果が依存しない / 分岐のあるエッジ列は弾く / 閉ループも扱える
- **頂点数の違うチェーン同士**を弧長で対応付け、両側の全頂点が必ず縫われる
- 端点同士が必ず対応する / 向きが逆のときに自動で反転判定する
- 退化ケース(全頂点が同一座標)でもクラッシュしない

### A-3. 性能ベースライン(M4 のGPU化判断材料)

CPU シングルスレッド / iterations=10, substeps=4 / Ryzen 環境:

| 頂点数 | ms/frame | 相当fps |
|--------|----------|---------|
| 441 | 0.5 | 1900 |
| 1,681 | 2.1 | 486 |
| 6,561 | 8.5 | 118 |
| 14,641 | 18.7 | 53 |

コリジョンのコスト(6,561頂点):

| 条件 | ms/frame | 相当fps |
|------|----------|---------|
| コリジョンなし | 8.3 | 120 |
| 球コライダー(1,536三角形) | 10.4 | 96 |
| 自己衝突のみ | 26.3 | 38 |
| 両方 | 25.1 | 40 |

→ **約1.5万頂点までは CPU でリアルタイム(>30fps)**。BVHのおかげでコライダーの追加コストは
+25% 程度に収まっている。

> **この節の数字は古い (2026-09-15)。** 以下の変更で内訳が入れ替わっている。
> 最新の実測は [README.md](README.md) の「性能の目安」を見ること。
>
> - 空間ハッシュを計数ソート方式にして自己衝突が 6倍速くなった
> - 頂点-三角形の自己衝突を足したので、自己衝突は再び最大項目になった
>   (14,641頂点で 44.3%)。**ただし「GPU化の第一目標」という結論は無効**で、
>   いま制約求解と自己衝突のどちらを狙うべきかは測り直す必要がある

---

## B. 手動チェック(Blender必須)

> **大半はヘッドレステストで自動化済み** (`blender_selftest.py`、66項目)。
> ここは「自動テストが何を見ているか」を人間向けに書いたものとして残してある。
> 手で追う必要があるのは、冒頭の「手作業で残っているもの」の4件だけ。

準備: `powershell -ExecutionPolicy Bypass -File scripts/package_zip.ps1` を実行し、
Blender の Preferences > Add-ons > Install... から `dist/muslin.zip` を入れる。
既に古いバージョンが入っている場合は**先に削除してから**入れ直すこと
(`.pyd` が置き換わらないことがあるため)。

### CP-B1. アドオンが読み込める
- [ ] Preferences > Add-ons に "Muslin" v0.5.0 が出て、有効化してもエラーが出ない
- [ ] 3Dビューの N パネルに "Muslin" タブがあり、Solver / Fabric / Forces / Collision /
      Pinning / Pattern / Sewing / Bake / Debug のサブパネルが並ぶ
- [ ] 無効化 → 再有効化してもビューポートの縫い線描画が二重にならない

### CP-B2. Rust コアが繋がっている
- [ ] Debug > `Test Rust Core` を押す → コンソールに以下が出る
  ```
  [muslin] Hello from Rust (cloth_core)!
  [muslin] add(1, 2) = 3.0
  [muslin] core_version = 0.3.0
  ```
- [ ] `core_version` が **0.3.0** であること(それ以前なら古い `.pyd` が読まれている)

### CP-B3. Blender 内での自己診断
- [ ] Debug > `Run Self Test` を押す → コンソールに 4項目すべて PASS
      (free fall / hanging grid / seam closure / sphere collision)
- [ ] シーンが一切変更されていない(このテストはメモリ内だけで完結する)

### CP-B4. M1 Exit条件 — 布が垂れる
1. Plane を追加 → 編集モードで細分化(Subdivide)を 5〜6回(約 33×33 頂点)
2. 上端の頂点列を選択し、頂点グループ `pin` を作って割り当て
3. N パネル > Pinning で `pin` を選択
4. フレーム 1 に戻して `Start Simulation`
5. タイムラインを再生
- [ ] 布が重力で垂れ下がり、上端はピンで固定されたまま
- [ ] 発散(頂点が吹き飛ぶ)やクラッシュが起きない
- [ ] メインパネルの `Stretch error` が概ね 0.01 未満で推移する

### CP-B5. タイムライン操作の整合性
- [ ] 再生を止めて**フレームを巻き戻す**と、布の形も巻き戻る(初期姿勢に戻る)
- [ ] 一度再生した範囲を**スクラブ**すると、行き来しても同じ形になる(キャッシュが効く)
- [ ] `Cache Frames` を切ると巻き戻しが再計算になる(遅くなるが結果は同じ)
- [ ] 大きくフレームを飛ばしても Blender が固まらない
      (600フレーム以上先へ飛ばした場合は再計算を諦めて現状維持する仕様)

### CP-B6. パラメータの効き方
- [ ] `Bending Compliance` を上げると布が柔らかくなる(細かいシワが増える)
- [ ] `Damping` を上げると早く静止する
- [ ] `Wind` の X に 2.0 程度を入れると布が横になびく
- [ ] `Density` を上げると、**同じ風でもなびきが小さくなる**(垂れ方自体はほぼ不変。
      これは物理的に正しい挙動)
- [ ] `Floor Collision` を有効にし `Floor Z` を布の下に置くと、布が床で止まる

### CP-B7. M3 縫製ワークフロー — 2枚のパターンを縫い合わせる
1. Pattern パネルで `Width` / `Height` / `Resolution` を設定し `Add Pattern Piece` を2回
   （3Dカーソルを動かして左右に離して配置）
2. 両方を選択して `Join Pattern Pieces`（縫うピースは同一オブジェクトである必要がある）
3. 編集モードに入り、**左ピースの右端エッジ列と、右ピースの左端エッジ列の両方**を選択
4. Sewing > `Add Seam From Selection`
- [ ] 「Seam 1: N ↔ M 頂点 / K ペア」と表示され、リストに1行追加される
- [ ] **ビューポートに縫い線がオレンジで表示される**（アクティブなシームは黄色）
- [ ] 縫い線が斜めに交差していない（交差していたらリストの ⇔ ボタンで反転）
- [ ] `Validate Seams` が「すべて正常」と報告する
- [ ] 3本以上のエッジ列や分岐したエッジを選んで `Add Seam` するとエラーで弾かれる
- [ ] `Start Simulation` → 再生すると `Seam Close Frames` かけて2枚が引き寄せられ、
      縫い目でくっつく
- [ ] 縫合後に布が破綻（裏返り・爆発）しない
- [ ] リストの ☑ を外すとその縫い目だけ無効になる

### CP-B7b. パターン作成と応用
- [ ] `Resolution` を小さくすると `Add Pattern Piece` の頂点数が増える
- [ ] 編集モードで閉じた輪郭（多角形のエッジループ）を選び `Fill Outline as Pattern`
      → 内部が三角形で埋まり、目標エッジ長まで細分化される
- [ ] **頂点数が異なる2本のエッジ列**を縫い合わせても、全頂点が縫われて破綻しない
      （弧長で対応付けているため）
- [ ] 円筒形になるように2辺を縫い合わせる（袖を作る）と、筒状に閉じる
- [ ] `Show Seams` を切ると縫い線が消える
- [ ] `Join Pattern Pieces` を既存の縫い目があるオブジェクトに実行すると、
      警告とともに縫い目が削除される（頂点番号が変わるため。これは仕様）

### CP-B9. M2 コリジョン — 人体に着せる(M2 Exit条件)
1. UV球(または人体アバター)を配置し、その上に細分化した Plane を置く
2. N パネル > Collision > `Collider` にその球/アバターを指定
3. `Thickness` を布のエッジ長より小さく（例 0.005〜0.01）
4. `Start Simulation` → 再生
- [ ] 布が球/人体の表面で止まり、**貫通しない**
- [ ] メインパネルの `Contacts` が 0 より大きくなる（接触が検出されている）
- [ ] `Surface Friction` を 0 に近づけると布が滑り落ち、1 に近づけると留まる
- [ ] `Colliders N (M tris)` の三角形数が対象メッシュと一致する
      （サブディビジョンサーフェス等のモディファイア適用後の数になる）
- [ ] 複数のコライダーを `Collider Collection` で指定しても動く

### CP-B10. 自己衝突・動くコライダー
- [ ] `Self Collision` を有効にして布を折り重ねる → 布同士が貫通しない
- [ ] `Self Thickness` をメッシュのエッジ長より**大きく**すると布が膨らんで破綻する
      （これは仕様。エッジ長より小さくすること）
- [ ] 自己衝突を有効にすると明確に重くなる（自動計測では約3倍）
- [ ] アーマチュアで動く人体を `Collider` にし、`Animated Collider` を有効にすると
      布が体の動きに追従して押される
- [ ] `Animated Collider` を有効にしたまま**大きくフレームを飛ばす**と、
      追いつき計算中のコライダー形状が現在フレームのもので固定される（既知の制約。
      不自然になったら一度停止して開始し直す）

### CP-B12. M6 ベイク
1. シーンのフレーム範囲を設定し、布の設定を済ませる
2. Bake パネルの `Bake to Disk`
- [ ] プログレスが進み、完了後に「N フレーム / X MB」と保存先が報告される
- [ ] `.blend` を保存した状態なら、その隣に `blendcache_muslin/` が作られる
      （未保存なら一時領域。パネルに警告が出る）
- [ ] 再生・スクラブが**即座に**追従する（再計算が走らない）
- [ ] `.blend` を保存 → Blender を再起動 → 開いてもベイク結果が残っている
- [ ] ベイク範囲外のフレームでは端の形状で固定される
- [ ] `Free Bake` でキャッシュが消え、ボタンが `Bake to Disk` に戻る
- [ ] メッシュを編集して頂点数を変えると、再生時にコンソールへ
      「頂点数が一致しません」と出てベイク状態が解除される（クラッシュしない）
- [ ] **動くコライダー**（アーマチュアで動く人体）でベイクすると、
      ライブ再生時よりも正しく追従する（ベイク中は各フレームを実際に評価するため）
- [ ] `Convert to Shape Keys` を実行 → シェイプキーとキーフレームが作られ、
      **アドオンを無効化しても**再生できる
- [ ] 500フレームを超えるベイクで `Convert to Shape Keys` を押すとエラーで止まる（仕様）

### CP-B13. 入力バリデーション
- [ ] 頂点0個のメッシュで `Start Simulation` → 「頂点がありません」で中止される
- [ ] 面のないエッジだけのメッシュ → 警告付きで動く（一様質量になる）
- [ ] 孤立頂点のあるメッシュ → 警告が出る（その頂点は落下し続ける）
- [ ] オブジェクトのスケールが非一様な状態で開始 → Apply Scale を促す警告が出る

### CP-B11. 異常系
- [ ] メッシュ以外(カーブ等)を選ぶと `Start Simulation` が押せない
- [ ] 頂点0個のメッシュで開始してもクラッシュせずエラー表示で済む
- [ ] シミュレーション中にオブジェクトを削除しても Blender が落ちない
- [ ] アドオンを無効化 → 再有効化してもハンドラが二重登録されない
      (再生してもカクついたり2倍速で進んだりしない)
- [ ] コライダーに指定したオブジェクトを削除してもクラッシュしない
- [ ] 布自身をコライダーに指定しても無視される(自己衝突オプションを使うべき)

### CP-B14. 2026-09-14 のコードレビューで直した箇所の確認
自動テストでは検証できない(bpy 依存の)修正なので、実機で必ず確認する。

- [ ] **ベイクした .blend を保存 → Blender を再起動 → 開く** → タイムラインを
      再生するとベイク結果が再生される(`@persistent` 修正の確認。
      **修正前はここが黙って動かなかった**)
- [ ] 別の .blend を開いても、前のファイルのシミュレーションが残って暴れない
- [ ] シミュレーション実行中にオブジェクトを**リネーム**しても計算が続く
      (`session_uid` キー化の確認。修正前は状態を見失っていた)
- [ ] シームを作ったオブジェクトを**複製**しても、双方の縫い線が正しく描画される
- [ ] シームを作った後に編集モードで頂点を削除 → 縫い線の描画がエラーを吐かない
      (コンソールに IndexError が出ないこと)
- [ ] 1万頂点程度の布で再生し、修正前より体感が軽くなっている(numpy 化の確認)
- [ ] 非一様スケールのオブジェクトで開始 → 警告は出るが、形状が壊れずに落下する
      (座標変換の numpy 化でせん断・スケールの扱いを変えていないことの確認)

---

### CP-B15. 境界値・パラメータ組み合わせ (2026-09-15 追加)

**ヘッドレスで測って境界が分かっている項目**を、実機で確かめる。値を適当に
振るのではなく、**壊れる値のすぐ手前と、すぐ向こう**を狙うのが効率的。
「壊れる側」で実際に壊れなければ、こちらの測定か実機の前提が間違っている。

以下の表の「辺長」は `Fit Thickness to Mesh` が見る中央値(スケール適用後)。
細かいメッシュほど許容値が小さくなる点に注意。

#### B15-1. Convergence Boost × Iterations(今セッションで直した箇所)

布を吊るし(上端ピン)、`Substeps` 8。**修正前は反復を上げると布が吹き飛んだ**。

| Iterations | Boost | 期待される結果 |
|---|---|---|
| 10 | 0.98 | 普通に垂れる(加速あり) |
| 20 | 0.98 | 普通に垂れる。**修正前はここで既に崩れ始めていた** |
| 40 | 0.98 | 普通に垂れる。**修正前は 0.4m の布が 12m まで広がった** |
| 40 | 0.0 | 普通に垂れる(比較用の基準) |
| 5 | 0.98 | 加速が一切効かない(構造的に無効。0.0 と同じ挙動) |

- [ ] 反復 40 / Boost 0.98 で布が吹き飛ばない
- [ ] 反復を 10 → 20 → 40 と上げるほど、布が**硬く**なる(逆転しない)
- [ ] Boost を 0 → 0.95 → 0.98 と上げるほど硬くなる(逆転しない)
- [ ] どの組み合わせでも `Stretch error` が跳ね上がらない

#### B15-2. Self Thickness × 辺長

床に落として堆積させる。**厚みが辺長に近づくと布が自分で膨らむ**。

| Self Thickness / 辺長 | 実測(最長辺/静止長) | 期待される結果 |
|---|---|---|
| 0.4(`Fit Thickness` の値) | 1.000 | 正常。膨らまない |
| 0.6 | 1.000 | 正常だが**警告が出る**境界 |
| 0.8 | 1.000(広がり +14%) | 崩れ始め |
| 1.0 | 1.053 | 見て分かる膨らみ |
| 2.0 | 2.677 | 明らかに破綻 |

- [ ] `Fit Thickness to Mesh` を押した値で膨らまない
- [ ] 辺長の 0.6倍を超える値を入れて `Start Simulation` → **警告が出る**
- [ ] 辺長の 2倍を入れると、実際に布が膨らむ(警告が空振りでないことの確認)

#### B15-3. Catch Fast Motion × 落下速度 × Substeps

布を別の布の上に落とす。**抜けない上限速度 = Self Thickness x fps x Substeps**。
`Self Thickness` 0.016 / 60fps / `Substeps` 4 なら 3.84 m/s。
落下高 h から落ちる速度は v = sqrt(2gh)(1m で 4.4 m/s、3m で 7.7 m/s)。

| Substeps | 落下高 | Catch Fast Motion | 期待される結果 |
|---|---|---|---|
| 4 | 0.5m (3.1 m/s) | 切 | 抜けない(上限以下) |
| 4 | 1m (4.4 m/s) | **切** | **素通りする** |
| 4 | 1m (4.4 m/s) | 入 | 抜けない |
| 4 | 7m (12 m/s) | 入 | 抜けない |
| 4 | 45m (30 m/s) | 入 | 一部抜ける(既知の限界) |
| 16 | 7m (12 m/s) | 切 | 抜けない(上限 15.4 m/s) |

- [ ] `Catch Fast Motion` を切ると、落下高 1m で実際に素通りする
- [ ] 入れると同じ条件で止まる
- [ ] 通常のドレープで体感が重くならない(実測 +10%、Substeps 16 なら +1%)

#### B15-4. Substeps × Bending Compliance(硬い生地)

| Fabric | Substeps | 期待される結果 |
|---|---|---|
| Denim / Leather | 4 | **警告が出る**。生地の差がほぼ出ない(垂れ比 1.02〜1.04) |
| Denim / Leather | 16 以上 | 硬さが見える |
| Chiffon / Silk | 4 | 警告は出ない(柔らかい生地は元々垂れる) |

- [ ] Denim を選んで `Substeps` 4 のまま開始 → 警告が出る
- [ ] Fabric パネル上でも(開始前に)その場で知らされる
- [ ] `Substeps` を 16 に上げると、Denim と Chiffon の差が目で分かる

#### B15-5. 摩擦(既定値を 0.3 → 0.8 に変更済み)

球に布を被せる。**0.3 では布が滑り落ちる**。

| Friction | Substeps | 実測(球に載った頂点) |
|---|---|---|
| 0.3 | 4 | **0**(全部滑り落ちる) |
| 0.3 | 8 | **0** |
| 0.8 | 4 | 160 |
| 0.8 | 16 | 190 |

- [ ] 既定(0.8)で球に被さったまま止まる
- [ ] 0.3 に下げると滑り落ちる(既定値の変更が妥当だったことの確認)
- [ ] `Substeps` を変えても摩擦の効き方が変わらない(正規化済み)

#### B15-6. 見た目・触り心地(機械では判断できない)

数値では通っていても、人が見て変なら意味がない項目。

- [ ] 生地プリセットを切り替えて、**名前から想像する質感と合っている**
- [ ] シワの出方が不自然でない(規則的な格子模様になっていない)
- [ ] パラメータを動かしたときの反応が直感と合う(上げて柔らかくなる等)
- [ ] 縫い目の線がビューポートで見やすい
- [ ] パネルの並びで迷わない

---

## C. 既知の制約 / 次にやること

現時点で意図的に未対応の項目。CP-B の結果次第で優先順位を調整する。

| 項目 | 現状 | 対応予定 |
|------|------|----------|
| 自己衝突の精度 | 頂点同士 + 頂点-三角形。エッジ同士は未対応だが、**それでしか拾えない違反は実測 0.1%** なので実装しないと決めた。既定の Substeps 4 では厚みの 7.5% まで潰れる対が残るので **Substeps 8 以上が実用的な下限** | 完了 |
| 初期食い込みで布が吹き飛ぶ | **対処済み**。`Start Simulation` とベイクが開始前に `untangle` を通し、速度を出さずに食い込みを解消する(1.924m 吹き飛んでいたものが 0.020m で静止)。重なっていないシーンでは何も動かさないのでベイクの互換性は保たれる | 完了 |
| 走行中の深い食い込み | 厚み以上まで食い込むと押し出しが速度に変わって弾かれる。CCD が無いことと同じ話 | M4 |
| 高速移動時の貫通(コライダー) | 球 60 m/s・直径 2cm の棒 30 m/s でも当たる(サブステップ補間 + 移動量ぶん広げた探索半径)。布が薄い板に落ちる側も 12.5 m/s で抜けない | 対処済み |
| 高速移動時の貫通(布どうし) | **掃過判定で対処済み**(`Catch Fast Motion`、既定で有効)。上限が 3.84 m/s から 12 m/s に伸びた。費用は 14,641頂点・substeps 4 で +10%、substeps 16 で +1% 未満。残るのは**布どうしが互いに高速ですれ違う**場合だけ(三角形側は現在位置で見ているため) | 対処済み(残りは M4) |
| 動くコライダー + スクラブ | 飛ばしたフレームの追いつき計算中、コライダー形状が現在フレームで固定される | M6 |
| 曲げ剛性の反復数依存 | **曲げ制約が収束していない**ため、既定の Substeps 4 では compliance をどう設定しても垂れ比 1.02〜1.04(硬さの上限を決めているのは生地の設定ではなく solver)。広い間隔の制約を足す案は計測で退けた(ドレープが板になる)。硬い生地 + 低 Substeps は警告で知らせている。本筋の解決には階層解法が要る | 階層解法(M4以降) |
| パターンの2D編集画面 | 専用の2Dビューは無く、3Dビューの編集モードで作る | 将来 |
| ピース間の縫製 | 別オブジェクト同士は縫えない(事前に統合が必要) | 将来 |
| メッシュ編集後の縫い目 | 頂点番号が変わると縫い目が壊れる(`Validate Seams` で検出可) | M6 |
| サンプルシーン | `samples/` に3つ。自動生成・自動検証 | 完了 |
| Extensions Platform | 対応済み。従来形式の zip も併せて作れる | 完了 |
| GPU化 | 未着手。CPUで約1.5万頂点まで実用 | M4 |
| モディファイア対応 | ベースメッシュの頂点を直接書き換えている | M6 |
