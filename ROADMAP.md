# Blender Cloth Simulation Addon — ロードマップ

技術スタック: Python(Blender連携) + Rust(物理コア, PyO3/maturin) + wgpu(GPU compute)

進捗管理: 各タスクの `[ ]` を `[x]` に変更して更新してください。
実機(Blender)での確認項目は [CHECKPOINTS.md](CHECKPOINTS.md) に集約しています。
`[x]` = 自動テストで検証済み / `[~]` = 実装済みだが Blender 実機確認待ち。

---

## M0: 環境構築・技術検証
**Exit条件**: Blender上のボタン押下でRust関数が呼ばれ結果がコンソールに出る

- [x] Rust開発環境セットアップ(rustup, cargo) — rustc/cargo 1.98.0-nightly 導入済み
- [x] maturin導入、Blender同梱Pythonのバージョン確認 — maturin 1.12.6。**実機のBlenderは5.1、同梱PythonはCPython 3.13**(`%APPDATA%\Blender Foundation\Blender\5.1\scripts\addons\rust_gpu_sdf_addon\__pycache__\*.cpython-313.pyc` から確認)。今回のビルドはabi3-py311(Stable ABI、3.11以降で前方互換)なので3.13でも再ビルドなしで動作する想定
- [x] PyO3で最小Rust拡張モジュール作成(例: ベクトル加算関数) — [rust/cloth_core](rust/cloth_core) に `hello()` / `add()` を実装、abi3-py311でビルド
- [x] ビルド・配布方法の暫定方針決定(dev用ローカルビルド vs 事前コンパイル済みバイナリ同梱) — Blender配布版は`python.exe`を同梱しないため`maturin develop`は不採用。`maturin build`でwheel化し`.pyd`をアドオンフォルダに同梱する方式に決定。[scripts/build.ps1](scripts/build.ps1)で自動化
- [x] Blenderアドオンの骨格作成(bl_info, register/unregister) — [addon/muslin/__init__.py](addon/muslin/__init__.py)
- [x] Operatorクラス作成、ボタン押下でRust関数呼び出し — [addon/muslin/operators.py](addon/muslin/operators.py)
- [x] Panel(UI)にボタン配置し、Rustからの戻り値をコンソール出力 — [addon/muslin/panels.py](addon/muslin/panels.py)
- [x] アドオンを実アドオンフォルダに配置 — `dist\muslin.zip`(scripts/package_zip.ps1生成)をBlenderの Install から導入する運用に統一
- [x] Blender 5.1(Python 3.13)実機で動作確認済み(2026-07-17) — Nパネルのボタン押下でコンソールに以下が出力されることを確認:
  ```
  [muslin] Hello from Rust (cloth_core)!
  [muslin] add(1, 2) = 3.0
  ```
  abi3-py311ビルドのままPython 3.13でも再ビルド不要で動作することを実証

**M0完了 (2026-07-17)**

---

## M1: CPUベース基本クロスシミュレーション
**Exit条件**: Blender内で平面メッシュを布のように垂らして落下させられる

- [x] シミュレーション用データ構造設計(頂点位置・速度・質量、エッジ制約リスト) — [rust/cloth_core/src/sim.rs](rust/cloth_core/src/sim.rs)。物理コアは pyo3 非依存にして `cargo test` で検証可能にした
- [x] BlenderメッシュデータをRustに渡すI/F実装(頂点座標配列の受け渡し) — [addon/muslin/mesh_io.py](addon/muslin/mesh_io.py)。`foreach_get`/`foreach_set` 使用
- [x] 重力積分(semi-implicit Euler等)実装 — 自由落下が解析解と一致することをテストで確認
- [x] 伸び制約(distance constraint)実装 — XPBD方式。λをサブステップ内で累積する正式なXPBD
- [x] 曲げ制約(bending constraint)実装 — 当初は Provot方式(隣接面の対角頂点を距離制約で結ぶ)。**剛性の制御として機能しないことが分かり、M5 で平均曲率の一次形式に作り直した**(M5 の「曲げ制約の作り直し」を参照)
- [x] 固定頂点(ピン留め)機能実装 — 頂点グループ指定。inv_mass=0
- [x] フレームごとの更新頂点座標をBlenderメッシュに反映するコールバック実装 — `frame_change_post` ハンドラ
- [x] タイムライン再生・Bake再生との連携確認 — フレーム番号基準の決定的再生 + メモリキャッシュでスクラブ対応。**実機確認は CP-B5**
- [x] シミュレーションパラメータ(反復回数, dt, 重力値)をUIから調整可能に — Solver / Fabric / Forces サブパネル
- [~] 平面メッシュでの落下テストシーン作成・動作確認 — **CP-B4 で確認待ち**

### M1で追加実装した項目(当初計画外)
- [x] 面積×密度からの頂点質量算出(一様質量ではなく物理的な質量分布)
- [x] 速度減衰(damping)
- [x] 風(面積に働く力として実装。密度が高いほどなびきにくい)
- [x] 床面衝突 + 摩擦(M2の本格コリジョンまでの暫定機能)
- [x] 発散検知・例外の握りつぶし(Rust側パニックでBlenderを落とさない)
- [x] 品質指標 `average_stretch_error()` をUIに表示
- [x] Blender内自己診断オペレータ `Run Self Test`

---

## M2: 衝突判定(自己衝突 + コリジョンオブジェクト)
**Exit条件**: 布が人体メッシュに引っかからず自然に覆いかぶさる

- [x] コリジョンオブジェクト指定UI(対象メッシュをBlender上で選択) — Collision パネル。単体オブジェクト + コレクション指定。モディファイア/アーマチュア適用後の評価済みメッシュを使用
- [x] 布-コリジョン間の距離判定・押し出し応答実装 — 三角形最近接点 + 面法線で内外判定し厚み分押し出す
- [x] BVH(Bounding Volume Hierarchy)構築ロジック実装(Rust側) — [rust/cloth_core/src/collision.rs](rust/cloth_core/src/collision.rs)。中央値分割 + 明示スタック探索。総当たりとの一致をテストで確認。動くコライダー用に refit も実装
- [x] 自己衝突検出用spatial hashing実装 — 一様グリッドハッシュ
- [x] 自己衝突応答(頂点押し戻し)実装 — 制約で結ばれた頂点対は除外(伸び制約との綱引き防止)
- [x] 摩擦(簡易)実装 — 接触法線方向の食い込み速度を除去し接線速度を減衰
- [x] パフォーマンス計測基盤作成(頂点数/フレーム時間ログ出力) — `verify_core.py --bench`
- [~] 人体メッシュ(サンプルアバター)を使った被せテスト・調整 — **CP-B9 で確認待ち**(自動テストでは球コライダーで検証済み)

---

## M3: 縫製パターン機能
**Exit条件**: 2枚の布ピースを縫い合わせてシャツの袖のような立体を作れる

- [x] 2Dパターンピース表現方法の決定(平面メッシュ or カーブベース) — **平面メッシュ**を採用。Blenderの編集ツールをそのまま使え、シミュレーション対象と同一表現で済むため
- [x] パターン編集モード(専用Operator/モーダル)の実装 — `Add Pattern Piece`(サイズ・解像度指定の長方形)と `Fill Outline as Pattern`(閉じた輪郭を三角形分割し目標エッジ長まで細分化)。モーダルな独立モードは作らず、Blenderの編集モードを活かす方針
- [x] シーム(縫い目)指定ツール — エッジ選択でペア登録するUI — `Add Seam From Selection`。選択エッジを連結成分に分解し、ちょうど2本のチェーンをシームとして登録。オブジェクトに永続保存され、UIListで管理(有効/無効・反転切替・削除)。[addon/muslin/seams.py](addon/muslin/seams.py) の中核ロジックは bpy 非依存でテスト済み
- [x] シーム制約をXPBD制約として追加(縫い目の頂点を引き寄せる) — rest_length を initial→0 に補間して徐々に閉じる方式。**弧長で対応付ける**ので頂点数が違うチェーン同士も縫える
- [x] 複数パターンピース間の初期配置(3D空間への展開配置)機能 — ピースは通常のオブジェクトとして自由に配置し、`Join Pattern Pieces` で統合してから縫う運用
- [x] 縫製シミュレーション(徐々に閉じていくアニメーション)実装 — `Seam Close Frames` フレームかけて閉じる
- [x] 縫製後の縫い目破綻検知・デバッグ表示 — ビューポートに縫い線をGPU描画(アクティブなシームは強調表示)。`Validate Seams` で長さの食い違い・無効な頂点参照を検査
- [~] サンプル衣服(袖、簡易シャツ)での動作確認 — **CP-B7 で確認待ち**

---

## M4: GPU化
**Exit条件**: 同一シーンでGPU版がCPU版比◯倍以上高速、かつ結果の視覚的差異が許容範囲

- [ ] wgpu導入、Rustプロジェクトへの統合
- [ ] GPUバッファへの頂点データ転送実装
- [ ] XPBD constraint solverのcompute shader実装(WGSL)
- [ ] 衝突判定のGPU化(spatial hashingのGPU実装)
- [ ] CPU版/GPU版の結果一致性検証(許容誤差設定)
- [ ] ベンチマークスイート作成(頂点数別の処理時間比較)
- [ ] CPU/GPU自動切替ロジック(頂点数閾値 or ユーザー指定)実装
- [ ] 大規模メッシュ(数万頂点)でのリアルタイム性検証

---

## M5: マテリアル物性・品質向上

- [x] 物性パラメータのデータモデル — シーン単位の `muslin_props`
  (密度・伸び・曲げ・減衰・摩擦)。マテリアル単位の管理は未対応
- [x] 生地プリセット — Chiffon / Silk / Knit / Cotton / Wool / Denim / Leather。
  密度は実際の目付(g/m^2)にもとづく。曲げの値はカンチレバー法で較正した
- [x] **曲げ制約を作り直した**(下記)
- [x] パラメータをオブジェクト単位で持てるようにした —
  `Object.muslin` に移した。シーンに残したのは時間・縫い線表示・
  パターン作成の道具設定だけ。これで1シーンに複数の生地を置ける
- [ ] さらにマテリアル単位(1オブジェクト内で部位ごとに生地を変える)まで進めるか検討
- [x] 風力・外力エフェクトの実装 — 一様風のみ(N/m²の面圧として実装)。乱流・Force Field連携は未対応

#### 曲げ制約の作り直し (2026-09-15)

生地プリセットを作ったが、**どの生地も同じように垂れて差が出なかった**。
調べると曲げ制約そのものが機能していなかった。3案を実装・計測して決着させた。

| 方式 | 結果 |
|---|---|
| 距離制約(Provot) | **剛性の制御として機能しない**。完全剛体でも 22cm の短冊が 21.85cm 垂れた(制約なしで 22.18cm)。曲げ角に対する長さ変化が θ²/8 と二次で感度が低すぎる |
| 二面角(acos) | **平ら付近で数値破綻**。勾配に 1/sin が入る。compliance 1e3 未満で発散(伸び誤差 10^10)、平ら付近を除外すると剛性が消える |
| **平均曲率の一次形式** | **採用**。三角関数も 1/sin も無い |

採用した形は、静止状態で平面になる4頂点に対し `Σ K_i = 0` かつ
`Σ K_i x_i = 0` を満たす重み K を求め、`C = |Σ K_i x_i|` を制約とするもの。
`C = ½|v|²`(二次)も試したが、分母が `|v|²` に比例して α̃ が支配的になり
compliance の制御が効かなかったため一次にした。

**曲げ剛性は PBD の反復数に依存する**。完全剛体でも垂れ比は
iter20/subs8 で 0.94、iter200/subs8 で 0.76、iter20/subs32 で 0.65、
iter200/subs32 で 0.11 と変わる。ここでもサブステップのほうが効率が良い
(640回の内側求解が 1600回より良い結果)。

**実装の途中で3つの誤りを計測で捕まえた**:
- 二面角の頂点の並び順の規約を取り違えていた(共有辺は p1-p2、p2-p3 ではない)
- 参考文献の勾配の式は `-∇d` を返すのに `+∇d` と仮定していた。
  補正が静止角から遠ざかる向きに働き、反対側へ折り返していた。
  数値微分と突き合わせるテストを先に書いておいたおかげで特定できた
- 円形試料のドレープ係数で検証しようとしたが、同心円で張った三角形が
  外側の輪を埋めきれず穴だらけで、どの生地も同じ結果になった。
  正方格子のカンチレバー法に切り替えた

**互換性**: 曲げの compliance は意味が変わった。旧 Provot 方式の値
(1e-4 前後)は無意味で、新しい有効範囲は 0〜0.3。既定値を 0.02 にした。

**曲げのコスト**: 新しい制約は1つあたり4頂点に触るので、旧方式より4倍ほど高価。
14,641頂点でフレーム時間の 37.9% を占める(最大項目)。
「曲げは伸びほど反復を必要としないのでは」と考えて回数を分けられるようにして
測ったが、**反証された**。完全剛体でのカンチレバーの垂れ比は反復20回で 0.803、
10回で 0.851、3回で 0.912 と、減らすほど柔らかくなるだけだった。
速度と引き換えに柔らかくするなら compliance を上げれば済むので、
パラメータは撤回した。

#### 自己衝突を点検した (2026-09-15)

「ちゃんと機能しているか」を確かめるため、コードを精読して計測した。
**機能はしているが、問題を3つ見つけた。**

**1. 既存のテストは自己衝突を一度も起動していなかった。**
`self_collision_keeps_cloth_stable`(Rust)も `test_self_collision`
(verify_core)も、上端を固定した平らなグリッドを垂らすだけ。180フレームで
**接触数は 0** だった。どちらも「発散しない」「伸び誤差が小さい」しか見て
いないので、`resolve_self_collisions` が即 return しても通る。
→ 重なった2枚を押し離す試験と、崩落させて折り重ねる試験を追加した。

**2. 摩擦が接触数ぶん累乗で掛かっていた(修正済み)。**
摩擦ループが接触ごとに速度を更新していたため、接線速度に `(1 - friction)`
が接触の個数ぶん掛かっていた。自己衝突では1頂点が多数の対に現れるので効く:

| 接触対 | 実測の残存率 | (1-f)^K | 正しい値 (1-f) |
|---|---|---|---|
| 1 | 0.6998 | 0.7000 | 0.7000 |
| 2 | 0.4908 | 0.4900 | 0.7000 |
| 4 | 0.2409 | 0.2401 | 0.7000 |
| 8 | 0.0592 | 0.0576 | 0.7000 |

摩擦の強さが「たまたま何対に含まれたか」= メッシュ密度と局所配置で決まって
しまい、折り重なった布が不自然に固着する。**頂点ごとに1回**に変更した
(法線の和を正規化して代表の法線にする。接触が1つなら従来と同じ)。
食い込む向きの法線速度の除去だけは、どの接触面にも入らないよう接触ごとに残す。

修正の前後で分離の品質は変わらない。15x15 の崩落では最終形状がビット単位で
一致し(摩擦は運動中の接線速度に効くので、静定後には差が出ない)、25x25 では
厚み違反が 234 → 254対、最小距離 0.00120 → 0.00115 とわずかに動いた。
固着しなくなったぶん布が動けるようになったためで、悪化ではない。

**3. 初期状態の食い込みが、そのまま運動エネルギーになる(未修正)。**
速度は位置差から作るので(`v = (pos - prev_pos) / dt`)、押し出した距離が
そのまま速度になる。**分離速度 = 食い込みの深さ ÷ サブステップの dt**:

| 食い込み | 分離速度 |
|---|---|
| 0.001 | 0.240 m/s |
| 0.005 | 1.200 m/s |
| 0.010 | 2.400 m/s |
| 0.016 | 3.840 m/s |

厚み 0.02 に対して 0.016 食い込んだ2枚は 3.84 m/s で吹き飛び、0.5秒で
1.9m 離れる。**パターンを重ねて置いてから縫う**、取り込んだ衣装が最初から
交差している、といった実際に起きる場面で問題になる。床・オブジェクト衝突も
同じ仕組みなので同様。

**対処した (2026-09-15)。** 分離速度を一律に非弾性にする案は採らなかった。
それだと**動くコライダーが布を押し出せなくなる**(アバターの動きに布が
付いてこない)。食い込みは開始時点に限った問題なので、開始前に片付ける:

`ClothSim::untangle` は `step` を通さずに衝突解決だけを回して位置を直す。
速度は「位置の差 ÷ dt」からしか作られないので、ここで動かしたぶんは速度に
ならない。`build_cloth_sim` が `Start Simulation` とベイクの両方で必ず通す。

| | untangle なし | あり |
|---|---|---|
| 厚み 0.02 に 0.016 食い込んだ2枚、30ステップ後 | 1.9240 m | **0.0200 m** |

**重なっていないシーンでは1つも動かさない**ことをテストで固定した
(`untangle_does_nothing_when_nothing_overlaps`)。既存のベイクとの互換性は
この性質に乗っている。解消しきれないほど深く絡んでいる場合は警告を出す。

開始時に1回だけ走る。重なりが無ければ初回の走査で 0 件と分かって抜けるので、
コストはほぼ無い(14,641頂点で 1.01ms。同じメッシュの step 1回が 24.00ms)。

なお**走行中**に厚み以上まで食い込んだ場合は、依然として押し出しが速度に
変わる。こちらは CCD が無いことと同じ話(M4)。

**機能そのものは効いている。** 垂直な布を床に崩落させて、制約で結ばれて
いない頂点対のうち厚みより近いものを数えた(25x25、厚み 0.016):

| 自己衝突 | 厚み違反の対 | 最小距離 |
|---|---|---|
| なし | 4900 | 0.00000(完全に重なる) |
| あり | 234 | 0.00120 |

95% 減る。ただし既定の Substeps 4 では最小距離が厚みの 7.5% まで潰れる対が
残る。Substeps を上げると解消する(8 で 89.7%、16 で 97.6%)。
**自己衝突を使うなら Substeps 8 以上が実用的な下限**と言える。

#### 摩擦の Substeps 依存を直し、既定値を較正し直した (2026-09-15)

貫通の計測中に気づいた。**既定の設定では、球に落とした布が滑り落ちて
床まで行く。**

21x21 の布を半径 0.3 の球に落とし、球の表面付近に残った頂点を数えた:

| substeps | friction | 球に載った頂点 |
|---|---|---|
| **4(既定)** | **0.3(既定)** | **0** |
| 4 | 0.8 | 160 |
| 8 | 0.3 | 0 |
| 8 | 0.8 | 178 |
| 16 | 0.3 | 165 |
| 32 | 0.3 | 181 |

**原因は摩擦の適用方法。** 摩擦はサブステップごとに1回掛かるので、
1フレームでの接線速度の残存率は `(1-f)^substeps` になる。
既定の f=0.3 なら substeps 4 で 0.24、substeps 16 で 0.0033 で **70倍**違う。
接触数ぶん累乗になっていた問題(修正済み)と同じ構造が、サブステップ数にも
残っていた。Convergence Boost では直らない(制約ソルバの加速で摩擦とは無関係)。

**着せ替えが目的のアドオンで、既定設定だと服が体に留まらない**ということなので
軽くない。ただし直し方が分かれるので、手を付ける前に決める必要がある:

1. **基準サブステップで正規化する** — 1サブステップあたり `(1-f)^(4/substeps)`。
   既定(substeps 4)の結果はビット単位で変わらず、他のサブステップ数が
   既定と同じ摩擦になる。**ただし substeps 16 で今きれいに被さっている人の
   結果は変わる**(滑るようになる)
2. **既定の friction を上げる** — 0.3 → 0.76 程度で substeps 4 でも留まる。
   substeps 依存はそのまま残る
3. **両方** — 正規化したうえで、既定が今と同じ見え方になる値に再較正する

1 は「パラメータの意味を安定させる」、2 は「既定の体験を直す」で目的が違う。

**3 を採った。**

**正規化**: 1サブステップあたりの残存率を `(1-f)^(4/substeps)` にした。
基準を既定の Substeps (=4) に置いたので、**既定のシーンの結果は指数が
ちょうど 1 になって変わらない**。他のサブステップ数が既定と同じ摩擦になる。

床の上を滑らせて1フレームの減速を測り、substeps 1/2/8/16/32 で
既定(4)との差が 10% 以内に収まることをテストにした。

**既定値**: 0.3 → 0.8。球に載った頂点数で較正した(substeps 4):

| Surface Friction | 球に載った頂点 |
|---|---|
| 0.3〜0.6 | 0(全部滑り落ちる) |
| 0.7 | 115 |
| **0.8** | **160** |
| 0.9 | 170 |

0.8 を選んだのは、閾値(0.6〜0.7)から十分離れていて、かつ Substeps を
振っても安定するため:

| Surface Friction | subs4 | subs8 | subs16 | subs32 |
|---|---|---|---|---|
| 0.3 | 0 | 0 | 177 | 0 |
| 0.6 | 0 | 71 | 44 | 104 |
| **0.8** | **160** | **166** | **174** | **180** |

0.3 と 0.6 の散らばりは、正規化の失敗ではなく**閾値付近で結果が二値的に
跳ねる**ため。0.8 のように安定領域に入ると揃う。なお正規化後も Substeps を
上げると布が留まりやすいのは、摩擦ではなく接触解決そのものが良くなるから
(これは正しい挙動)。

**影響**: 摩擦を明示的に設定していたシーンは挙動が変わる。特に Substeps を
上げて使っていた場合は摩擦が弱くなる(今までが効きすぎていた)。

明示的に設定していないシーンは、**新しい既定値を自動で拾う**。Blender は
`bpy.props` の既定値と同じ値を .blend に保存しないので、読み込み時に
そのときの既定値が入る。サンプル3本を確認したところ、いずれも
`friction=0.80 / surface=0.80` になっていた(`02_flag` の `substeps=16` の
ように明示設定した値はそのまま保持される)。サンプルの作り直しは要らなかった。

#### 高速移動時の貫通を調べ、コライダー側を直した (2026-09-15)

「CCD が無いので高速に動くと貫通する」と書いてあったが、**どこがどれだけ
抜けるのかを測っていなかった**ので調べた。

**布側は抜けなかった。** 薄い板(1枚のグリッド)に布を落とし、落下 8m
(着地 12.5 m/s)まで試したが、板より下に行った頂点は 0。厚みを 0.005 の
まま、substeps 4 でも同じ。布は連結しているので、一部の頂点が捕まると
伸び制約でシート全体が減速する。**単独の質点で考えた閾値は当てはまらない。**

**抜けたのはコライダー側。** コライダーの座標はフレーム頭で1回しか
更新されない(`_update_animated_colliders`)。球(直径 0.30m)を通すと、
開始位置(位相)によって布に触れずに通過した:

| 球の速度 | 1フレームの移動 | 素通りした位相 |
|---|---|---|
| 15 m/s | 0.250 m | 2/8 |
| 30 m/s | 0.500 m | 3/8 |
| 60 m/s | 1.000 m | 5/8 |

**原因は2つあった。** 1つはフレーム単位の標本化。もう1つは探索半径で、
`max(厚み x 4, 厚み + 0.02)` = 0.025 しかないため、**大きなコライダーの
内部深くに入った頂点は表面が遠くて検出されない**。球の中心から 0.06 の
頂点は表面まで 0.09 あり圏外だった。細かく標本化しても、表面が頂点の
0.025 以内を通る瞬間を捉えないと押せない。

**対処**: `set_collider_target` でサブステップごとに線形補間し、
探索半径をそのサブステップの移動量ぶん広げた。

| 球の速度 | 修正前 | 補間のみ | 補間 + 半径拡大 |
|---|---|---|---|
| 15 m/s | 2/8 | 0/8 | **0/8** |
| 30 m/s | 3/8 | 4/8 | **0/8** |
| 60 m/s | 5/8 | 4/8 | **0/8** |

補間だけでは 30/60 m/s が直らず、半径拡大と合わせて初めて 0 になった。
**片方だけ入れて「効いた」と判断していたら、半分しか塞げていなかった。**

計測の作り方でも一度間違えた。最初はフレーム末尾の変位で判定していたが、
速い球は1フレーム以内に通過するので押した瞬間を取りこぼす。通過後に球を
遠ざけ、**残った揺れ**で判定する形に直した。

**残り**: CCD そのものは無い。ここで測ったのは球と平板だけなので、
細い棒や鋭い角のような形では未知。布どうしの高速な交差も未測定。

#### 曲げの収束を Chebyshev 加速で速めた (2026-09-15)

「制約を足す方向(梯子)では直らない。必要なのはエネルギーを足さずに補正を
伝える手法」と結論していた。階層解法(multigrid)は大掛かりなので、まず
**Chebyshev 半復法**(Wang 2015)を試したところ、十分な効果があった。

反復そのものを加速する手法なので**収束先 = 材質が変わらない**。これが梯子との
決定的な違い。実装は2項漸化式だけで済む:

    x^(k+1) = ω(x̃^(k+1) - x^(k-1)) + x^(k-1)
    ω: 1 → 2/(2-ρ²) → 4/(4-ρ²ω)

**Substeps と組み合わせたときに効く。** 内側の求解回数を揃えた比較
(カンチレバー、compliance=0、垂れ比。小さいほど硬い):

| 内側 | iter x subs | ρ | 垂れ比 | ms/frame |
|---|---|---|---|---|
| 320 | 10 x 32 | 0 | 0.7026 | 3.04 |
| 320 | 80 x 4 | 0 | 0.9244 | 3.00 |
| 320 | 80 x 4 | 0.95 | 0.8499 | 3.06 |
| 320 | 20 x 16 | 0.95 | 0.5954 | 3.08 |
| **320** | **10 x 32** | **0.95** | **0.3986** | **3.06** |
| 640 | 20 x 32 | 0 | 0.5069 | 6.03 |

**内側320回 + 加速(0.3986 / 3.06ms)が、内側640回の素の反復
(0.5069 / 6.03ms)より硬く、しかも半分のコスト。**

生地プリセットの差(7種類の垂れ比の幅):

| 設定 | 差 | 最も硬い生地 |
|---|---|---|
| iter10 x subs4(既定) | 0.016 | 1.024 |
| iter20 x subs32(これまでの最良) | 0.477 | 0.507 |
| iter10 x subs32 + ρ0.95 | 0.583 | 0.399 |
| iter20 x subs32 + ρ0.95 | 0.769 | 0.212 |

**反復が半分で、これまでの最良を上回る。**

**材質を変えていないことの確認**: 球に被せたときの水平の広がりは
substeps 32 で 0.576、+ρ0.95 で 0.526。柔らかい生地は**より巻き付く**
方向に動いた。梯子は同じ試験で 0.71 → 1.17 と板になったので、正反対。
硬い生地は硬く、柔らかい生地は柔らかく、どちらも本来の挙動に近づいている。

**既定は 0(無効)**。これまでのシーンの結果は変わらない。
ρ=0.99 では発散したので、UI の上限は 0.98 にした。

**既定の Substeps 4 では効果が小さい**(差 0.016 → 0.020)。反復10回では
加速する余地が少ないため。Substeps を上げてこそ効く、と README に書いた。

**残り**: 階層解法はまだ入れていない。Chebyshev で実用上の要求は満たせたので、
必要になったら着手する。

#### 頂点-三角形の自己衝突を実装した (2026-09-15)

自己衝突の点検で「頂点-三角形の貫通は完全には防げない」と書いてあった項目を、
**実際にどれだけ起きるのか測った**。結果は「完全に素通り」で、想定より遥かに悪い。

格子間隔 h の2枚を半セルずらすと、頂点間の面内距離が h√2/2 = 0.707h になる。
推奨の厚み 0.4h はこれより小さいので、**頂点同士は一度も反応しない**:

| 配置 | 上の層の最終 z | 下を抜けた頂点 |
|---|---|---|
| 頂点が揃っている | +0.0200(厚みで停止) | 0 |
| 半セルずらし | **-53.27** | **81 / 81** |

実際に折り重なった布で頂点が揃うことはまずない。つまり自己衝突は
**効いているように見えて効いていなかった**。先に測った「厚み違反 95% 減」も、
頂点同士が近い場合だけを数えた数字だった。

**実装**: 頂点と三角形の最近接点の距離を見て押し離す。押し出しは重心座標で
3頂点へ配分する。自分を含む面は除く。それ以外の近傍面は、厚みが推奨どおり
エッジ長の 0.4倍以下なら誤検出しない(平らな格子で最も近い非隣接面まで
0.707h あるため)。半セルずらしでも貫通ゼロになった。

**broadphase**: 三角形用の空間ハッシュを別に持つ。頂点用と違い、**挿入時に
厚みぶん広げて** AABB が跨るセル全部に入れる。こうすると参照は頂点のセル
1つで済み、27セル走査が要らない。三角形は頂点より数が少ないので、挿入を
厚くして参照を薄くするほうが釣り合う。

**メモリがそのまま速度になった。** 最初はセルの大きさを最大辺に合わせたが、
平らな布では1三角形が18セルに跨り、14,641頂点で毎サブステップ 3.7MB を
触っていた。その結果**自己衝突と無関係な伸び制約・曲げ制約まで 2.2倍に
膨らんだ**(キャッシュから追い出されるため):

| | 当初 | 削減後 |
|---|---|---|
| total | 64.92 ms | **33.53 ms** |
| 自己衝突 | 21.65 | 14.84 |
| └ ハッシュ再構築 | 14.99 | 9.97 |
| 伸び制約(無関係) | 21.11 | **9.37** |
| 曲げ制約(無関係) | 16.71 | **6.91** |

削ったのは3つ。セルを「広げたあとの三角形が軸あたり2セルに収まる」大きさに、
ハッシュ値を u64 から u32 に(弾くための印なので衝突しても候補が1つ増えるだけ)、
バケット数を 4T から 2T に。合計 3.7MB → 1.4MB。
**プロファイルを見ずに「ハッシュ再構築が重い」とだけ考えていたら、
巻き添えのほうが大きいことに気づけなかった。**

**コスト**: 自己衝突ありで 18.8 → 33.5 ms(1.8倍)。切れば元のまま。
トグルは付けなかった。「効かない自己衝突」は選択肢として意味がないため。

**残る穴**: エッジ同士の交差は見ていない。折り目が斜めに交差する場面では
まだ抜ける。頂点-三角形で大半は塞がるが、完全ではない。

崩落した 25x25 で、隣接しない対の違反を数えた:

| | 頂点-三角形 | エッジ-エッジ |
|---|---|---|
| 自己衝突なし | 33,586 | 54,650 |
| 自己衝突あり | 1,080 | 1,790 |

97% 減る。

**エッジ同士は実装しないことにした (2026-09-15)。** 上の「エッジ-エッジ
1,790」の多くは頂点-三角形の違反と同じ接触領域に重なっているはずなので、
切り分けて数え直した。判定は「4つの端点のいずれも、自分が属さない三角形の
厚み以内に無い」= 頂点-三角形からは見えない、というもの:

| 25x25 の崩落 | 件数 |
|---|---|
| エッジ-エッジの違反 | 2,116 |
| **うちエッジ同士でしか拾えない** | **2 (0.1%)** |

**99.9% は既に頂点-三角形で見えている。** 17x17 では 0 件だった。
エッジ同士を足しても増える網羅は 0.1% で、broadphase をもう1つ持つ
コストに見合わない。頂点-三角形を入れた時点で、自己衝突の網羅はここが
実用上の上限。

理屈のうえでは、辺だけが交差して頂点がどこにも近くない配置は作れる
(2本の細い帯を X 字に交差させる等)。ただし**布の崩落では実質起きない**。
面が連続しているので、辺が交差する場所には必ず面も近くにある。

**試して撤回した: 押し出しを複数回解き直す。** 押し出しは候補を1本ずつ順に
処理するので、後から動かしたぶんで前の対がまた食い込む。候補の集め直しも
ハッシュの作り直しも要らないので、同じ候補をもう一度舐めるだけなら安いはず、
と考えた。17x17 を崩落させて測った結果:

| 解き直す回数 | 厚み違反 | 最小距離(厚み比) | 自己衝突のコスト |
|---|---|---|---|
| 1 | 406 | 93.3% | 0.87 ms |
| 2 | 374 | 96.5% | 0.96 ms |
| 4 | 343 | 98.4% | 1.05 ms |

単調に良くなるが**小さい**。1回の時点で違反は厚みの 93.3% と浅く、
98.4% にしても見た目は変わらない。14,641頂点では +21% が約 +3ms になる。
**残っている違反は「浅く触れている」だけで、潰れているわけではない**ことが
分かったので撤回した。頂点-三角形を入れた時点で、この方向はもう頭打ち。

**ついでに見つけた NaN**: 上の調査で書いた計測スクリプトがゼロ除算で落ちた。
同じ式を使っている Rust の `closest_point_on_triangle` を確かめたところ、
**退化した三角形で NaN を返していた**。分母は順に |ab|²、|ac|²、|bc|²、
面積の2倍で、頂点が重なると 0/0 になる。教科書(Ericson)の式にはこの分岐が無い。

これは頂点-三角形の追加とは無関係に、**以前からオブジェクト衝突にあった**。
二重頂点のあるコライダーメッシュはユーザー環境でよくあるので、
「コライダーを指定したら布が消し飛ぶ」形で踏まれていた可能性がある。
4箇所すべてに下限を入れ、退化した5パターンで NaN が出ないことをテストにした。

#### 曲げ剛性の Substeps 依存を調べた (2026-09-15)

「既定の Substeps 4 では生地の差が出ない」の原因を切り分け、安い対策を
試して**退けた**。

**原因は compliance の意味ではなく、曲げ制約が収束していないこと。**
`compliance = 0`(完全剛体)でも垂れ比が solver 設定で動く:

| iterations x substeps | compliance=0 での垂れ比 |
|---|---|
| 10 x 4(既定) | 1.0238 — 垂れきる |
| 10 x 8 | 0.9542 |
| 10 x 16 | 0.8880 |
| 10 x 32 | 0.7026 |

反復を増やしても頭打ちにならない(突き出し36頂点で 1→320 反復まで単調に
改善し続け、改善幅はむしろ広がる)。**硬さの上限を決めているのは生地の
設定ではなく solver** で、既定では compliance 0〜0.3 のどれでも
垂れ比 1.02〜1.04 にしかならない。

**試して退けた対策: 広い間隔の曲げ制約(梯子)。** 間隔 s=2,4,8,16 の曲げ
制約を足せば、1掃引で s 頂点ぶん情報が飛んで収束が速まるはず、と考えた。
カンチレバーでは劇的に効いた(既定設定で垂れ比 1.0238 → 0.0733、14倍)。

**しかしドレープが壊れた。** 球に被せると布が巻き付かず板のまま広がる:

| 梯子 | 硬さ(カンチレバー垂れ比) | ドレープ(水平の広がり) |
|---|---|---|
| なし | 1.0238 | **0.7099**(正常) |
| s=2 | 0.9237 | 0.9762 |
| s=2,4 | 0.3511 | 1.1518 |
| s=2,4,8,16 | **0.0733** | 1.1699 |

硬さとドレープが完全なトレードオフで、**両立する設定は無かった**。最も弱い
s=2 でさえ硬さはほぼ改善しないのにドレープは悪化する。広い制約は収束を
速めているのではなく、**大スケールの剛性を足している**だけだった。
球への被せは二重曲率(非可展)を要求するので、「広い範囲で平らを保て」と
いう制約と真っ向からぶつかる。

→ **制約を足す方向では直らない。** 必要なのは、エネルギーを足さずに補正を
伝える本物の階層解法(Hierarchical/Multigrid PBD: 粗いレベルで解いて補正を
prolongate する)。これは M5 の範囲を超えるので次の大物として残す。

**当面の手当て**: 硬い生地(Bending Compliance が既定の 0.02 未満、Wool 以上)を選んだまま
Substeps が 16 未満なら `Start Simulation` で警告を出すようにした
([mesh_io.check_bending_stiffness](addon/muslin/mesh_io.py))。黙って
柔らかい布が出ると「プリセットが効いていない」と見えるため。
- [ ] Blender標準のForce Field連携検討

---

## M6: UX仕上げ・安定化・ドキュメント

- [x] シミュレーションキャッシュ/ベイク機能実装(ディスク書き出し) — `Bake to Disk`。1フレーム1ファイルのバイナリ形式(float32)。ヘッダに頂点数を持ち、メッシュ編集後の食い違いを検出する。[addon/muslin/cache_io.py](addon/muslin/cache_io.py) は bpy 非依存でテスト済み
- [x] ベイク済みアニメーションの再生・編集フロー確認 — フレーム変更時にキャッシュから復元。`Convert to Shape Keys` でアドオン非依存の形にも変換可能。**ベイク中は各フレームで `frame_set` するため、動くコライダーが正しく追従する**(ライブ再生時の追いつき計算にある制約が無い)
- [x] Rust側パニック時のフォールバック・エラーメッセージ表示実装 — ハンドラで例外を捕捉し、発散検知したら自動停止
- [x] 異常入力(非多様体メッシュ等)に対するバリデーション追加 — 頂点/エッジ無し、孤立頂点、非多様体エッジ、極小エッジ、非一様スケールを検査。致命的なものは開始を中止し、それ以外は警告表示
- [x] サンプルシーン作成 — `samples/` に3つ(ドレープ・旗・縫製)。
  [scripts/make_samples.py](scripts/make_samples.py) で生成し、
  ヘッドレステストで「開いて再生すると動く」ことを毎回確認している
- [x] README / インストール手順整備 — [README.md](README.md)
- [x] Blender Extensions Platform向けパッケージング対応 —
  `blender_manifest.toml` と [scripts/package_extension.ps1](scripts/package_extension.ps1)。
  ネイティブモジュールは wheel として宣言する。`blender --command extension validate`
  で検証し、一時領域にインストールして動作まで確認する

---

## メモ
- **ビルド時の注意**: PATH 先頭の `WindowsApps\python.exe` はスタブなので pyo3 が見つけられない。
  `scripts/build.ps1` が実体のある Python を探して `PYO3_PYTHON` に設定する。
- **PowerShell スクリプトは UTF-8 BOM 付きで保存すること**。Windows PowerShell 5.1 は
  BOM無しUTF-8をANSIとして読むため、日本語コメントが文字化けして構文エラーになる。
- 現状(2026-09-14): M1・M2・M3 完了、M6 も大半を実装。自動テスト(Rust 20 + Python 55項目)通過。
  Blender実機確認のみ未実施([CHECKPOINTS.md](CHECKPOINTS.md) の CP-B1〜B13)。
  **パターンを縫い合わせるワークフローの核心(パターン作成 → 縫い目指定 → 縫製シミュレーション → ベイク)が一通り繋がった状態**。
- **テスト網羅(2026-09-15 更新)**: `scripts/blender_selftest.py` で Blender を
  ヘッドレス起動し、アドオン層を検証するようにした(項目数と内訳は
  [CHECKPOINTS.md](CHECKPOINTS.md) を出典とする)。これで
  `operators.py` / `sim_state.py` / `mesh_io.py` / `bake_ops.py` / `sewing_ops.py` /
  `properties.py` が実際に動かされる。**残る未検証は `overlay.py`(ビューポート描画)と
  `panels.py` の見た目だけ**。
  なお bpy 非依存に切り出せるロジックは今後も切り出すこと(こちらのほうが速く回る)。
- コードレビューで見つけて修正した問題(2026-09-14):
  - `frame_change_post` ハンドラに `@persistent` が無く、**.blend を読み込むと
    ベイク再生が黙って止まっていた**。`load_post` で状態を捨てる処理も追加。
  - `overlay.invalidate_cache()` がどこからも呼ばれておらず、指紋もチェーン長しか
    見ていなかったため、メッシュ編集後に古い頂点番号で描画して例外を出す状態だった。
  - 実行中状態の dict がオブジェクト**名**キーで、リネーム・複製で迷子になっていた
    (`session_uid` キーに変更)。
  - ローカル<->ワールド変換が素の Python ループで、14,641頂点で往復約10ms
    (Rust の step 18.7ms に対して +54%)。numpy 化して往復約2.8msに短縮。
- 2026-09-14 の実機確認で分かったこと(実測は README「パラメータの決め方」に記載):
  - 自己衝突の `Self Thickness` の上限は**メッシュのエッジ長**で決まる(斜め方向の
    非拘束ペアが押し合うため)。超えると平らな布でも座屈し、伸び誤差が3倍になる。
    **細分化は解決にならず逆効果**。根本解決には頂点-三角形の自己衝突が要る(M4/M5)。
  - 収束は `iterations` より `substeps` のほうが安く効く(誤差 1/7 に対しコスト 0.77倍)。
    衝突解決がサブステップごとに1回・制約ループの外なので、**iterations では貫通は直らない**。
  - 衝突後に伸び制約を解き直す `post_collision_iterations` を追加(既定 2)。
    貫通を増やさず伸び誤差を減らす。
- **計測して対処済み(2026-09-15)**: 自己衝突のコストを 14,641頂点で **+41.3ms → +15.0ms(-64%)**
  に削減した。仮説(アロケーションが主因)は**外れ**で、真因は標準 HashMap の **SipHash** だった。
  セルキー `(i64,i64,i64)` に暗号学的ハッシュは過剰なので、乗算とシフトだけの
  `GridHasher` に差し替えた。近傍バッファの使い回しも 16% 分効いた。
  なお「バケットを残して中身だけ空にする」案は**悪化した**(+106ms)。布が動くと
  空バケットが溜まり、毎サブステップ全走査になるため。実装して測らなければ
  気づけなかった。
- **試して効果が無かったこと**(いずれも撤回済み):
  - 制約を頂点番号順に並べ替えてメモリ局所性を上げる案 — 29.62ms -> 29.41ms
    (0.7%、ノイズ内)。Blender のメッシュから取るエッジ順が既にキャッシュに
    優しい並びだった
  - 曲げ制約に `k` の二乗を持たせて掛け算を省く案 — **12.51ms -> 14.84ms と
    19% 悪化**。構造体が 32バイト太り、制約配列を流す量が 1.4MB 増えたほうが
    高くついた。**事前計算はデータが流れる場合タダではない**
  - 同じ求解ループを手で展開する案 — 12.51ms -> 13.88ms。配列を使った元の
    書き方のほうが最適化が効いていた。固定頂点を飛ばす分岐も失っていた
- **M4(GPU化)の前提が変わった**: 自己衝突はもう最大のコストではない。
  14,641頂点で自己衝突オフでも 23ms かかっており、**ボトルネックは制約ソルバ本体**に移った。

### 計測プログラムの結果 (2026-09-15)

`ClothSim::timings()` で step の内訳を取れるようにした(Debug > Print Timings)。
14,641頂点・自己衝突あり(合計 41.5ms)の内訳:

| 内訳 | 割合 |
|---|---|
| 自己衝突 | 40.3% (うち空間ハッシュ再構築 11.4%) |
| 伸び制約 | 25.7% |
| 曲げ制約 | 17.3% |
| オブジェクト衝突 | 9.0% |
| 衝突後の伸び補正 | 5.2% |

**距離制約の合計(伸び+曲げ+補正)が 48%** で、集計では制約ソルバが最大。
GPU化の的は「自己衝突」から「距離制約の並列化」に移すべき。

#### 実装して測った結果 (2026-09-15)

**距離制約の並列化は撤回した。** 見積もりでは 1.69倍だったが、実測では割に合わなかった:

| 頂点数 | 逐次 | 並列 | 速度比 |
|---|---|---|---|
| 4,225 | 5.08 ms | 11.69 ms | 0.43x |
| 14,641 | 21.24 ms | 16.25 ms | 1.31x |
| 40,401 | 60.40 ms | 25.38 ms | 2.38x |

生の速度比は大きいメッシュで伸びるが、**彩色 Gauss-Seidel は逐次版より収束が遅い**。
定常誤差が 121% 悪化し、同じ品質に届くには反復が 2.5倍要る。差し引きで
14,641頂点では **2.4倍遅い**(逐次 20.41ms に対し同品質の並列 49.09ms)。
過緩和(ω=1.2)で取り返せるか試したが、誤差はむしろ悪化した。

→ CPU での彩色 Gauss-Seidel は投資対効果が悪い。GPU なら状況が変わる
(スレッド数が桁違いで、バリアのコストも比較にならない)ので、彩色という
方向自体は M4 で有効。4色に塗り分けられて均等という性質はそのまま使える。

**代わりにオブジェクト衝突を並列化した。** 頂点ごとに独立で収束に影響しないため、
**品質の劣化なしに 3〜5.25倍**(フレーム全体で 1.36〜1.85倍)。コライダーが
密なほど効くので、人体アバターに着せる用途に効く。

~~**次の候補は自己衝突(40%)**~~。ただし頂点対が互いに書き込むので、彩色するか
Jacobi 的に補正を溜めてから適用するかの検討が要る。ペア集合はサブステップ毎に
変わるので、彩色を毎回作り直すコストも見積もること。

> **この節の内訳は古い。** 直後の `c704ede`(空間ハッシュを計数ソート化)で
> 自己衝突が 6.31ms → 3.01ms になり、構成比が入れ替わった。
> 現在は 曲げ 42.2% / 伸び 35.1% / 自己衝突 10.2%(README の表が最新)。
> **自己衝突はもう並列化の主目標ではない。**

#### 並列化の見込み(実装前の見積もり)

制約グラフを貪欲彩色して、並列化の条件を確認した(`scripts` 外の使い捨て計測):

| 項目 | 結果 |
|---|---|
| 必要な色数 | 伸び 4色 / 曲げ 4色(まとめて 8色) |
| 色の大きさ | 14,641頂点で 6,920〜7,320本(平均 7,230) — ほぼ均等 |
| 理論速度比 | 論理20コアで 19.99x(効率 100%) |
| **フレーム全体** | **1.69x** (アムダール則の上限) |

四角形グリッドの距離制約は **4色で塗り分けられ、各色がほぼ同じ大きさ**になる。
並列化の条件としては理想的。ただし実装前に確認すべき点が3つある:

- **論理20コアは物理コアではない**。ハイパースレッディング込みの数なので実効は下回る
- **メモリ帯域が先に律速する可能性**。1頂点あたり 3.2µs は計算より転送が支配的かもしれない
- **色ごとにバリアが入る**。8色 × iterations 10 × substeps 4 = 1フレーム320回。
  1回 1〜5µs なら 0.3〜1.6ms/frame の追加コストになる
- **彩色 Gauss-Seidel は逐次 Gauss-Seidel と収束が変わる**。品質の回帰を測る必要がある

**1.69x は距離制約だけを並列化した場合の上限**。それ以上を狙うなら自己衝突(40%)も
並列化する必要があるが、こちらは書き込み衝突があって彩色が効きにくい。

**計測環境の注意**: 同一バイナリ・同一設定でも実行間で 34% のばらつき(二峰性)が
出る。最小値を採る、同一実行内で比較する、といった手当てが必須。
実行をまたいだ絶対値の比較はできない。

**検証した仮説と結果**:
- Macklin et al., *Small Steps in Physics Simulation* (SCA 2019) の
  「反復より小さいサブステップ」は**このコードでも成立**した(衝突なしなら
  同コストで 12.6倍正確)。ただし**衝突を入れると逆転**し、既定の 10x4 より
  安くて正確な設定は無かった。**既定値は変更しない**のが正解だった。
- 衝突検出をフレーム先頭に1回まとめる案は、オブジェクト衝突には有効
  (Substeps 8以上で 1.1〜1.5倍、結果はビット単位で一致)。
  自己衝突には**有害**(候補が実接触の1000倍に膨れる)。
- 実装の途中で「候補は最近傍1個で足りる」「移動見込みは全体最大速度でよい」
  という2つの手抜きをしたが、どちらも計測で否定された(前者は食い込み 0.015、
  後者は 2〜3倍の速度低下)。移動見込みには安全係数 1.5 が必要。
- 残る大物は **M4(GPU化)** と M5(物性プリセット・二面角の曲げ制約)。
  M6 の残件はサンプルシーン作成と Extensions Platform 対応。
- 性能の目安: 6561頂点でコリジョンなし 8.3ms/f、球コライダー付き 10.4ms/f、
  自己衝突を入れると 26ms/f。~~**自己衝突が最大のコスト** = M4(GPU化)の第一目標。~~
  **この記述は無効**(2026-09-15)。空間ハッシュを計数ソートにした `c704ede` で
  自己衝突は 40.3% → 10.2% に落ち、**現在の最大項目は曲げ制約(42.2%)**。
  制約求解の合計が 84% を占める。M4 の的は「自己衝突」ではなく「制約求解」。
- 優先順位: M1→M2完了で最低限「製品として意味を持つ」状態。M3(縫製)がパターンを縫い合わせるワークフローの核心。M4(GPU化)は機能完成後の性能最適化フェーズ。
- 各マイルストーンの見積もりはM0: 1〜2週間 / M1: 3〜4週間 / M2: 4〜6週間 / M3: 4〜6週間 / M4: 6〜8週間 / M5: 3〜4週間 / M6: 3〜4週間(目安、要調整)。
