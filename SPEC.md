# Muslin — 仕様概要

Blender 用のクロスシミュレーションアドオン。Marvelous Designer のような
**型紙を作って縫い合わせ、着せて動かす**衣服制作ワークフローを、Blender の中で
完結させることを目指している。物理コアは Rust (XPBD) で書き、PyO3 経由で
Blender の Python から呼ぶ。

この文書は **2026-09-25 時点のまとめと今後の展望**である。
使い方・パラメータの実測値は [README.md](README.md)、進捗と退けた案は
[ROADMAP.md](ROADMAP.md)、検証項目は [CHECKPOINTS.md](CHECKPOINTS.md)、
先行事例の調査は [docs/references/taremin_cloth.md](docs/references/taremin_cloth.md) にある。

- アドオン v0.5.0 / `cloth_core` v0.3.0
- 動作確認: Blender 5.2.1 LTS / Windows（物理コアは Linux・macOS も CI で検証）
- ライセンス: GPL-3.0-or-later（`bpy` と結びつくため GPL 互換が必須）
- 到達点: M0〜M3・M6 完了、M5 は大部分、**M7（型紙から着せて動かす）・M8（型紙と連動する編集）・M9（CPU 並列化）達成**。M4（GPU 化）未着手

---

## 1. 何をするアドオンか

「モスリン（仮縫い）」の名のとおり、**平面の型紙を置き、辺同士を縫い、立体に
組み上がる過程をシミュレートする**。標準の Cloth モディファイアが「既にある立体
メッシュを揺らす」のに対し、こちらは**衣服の成り立ちのほう**を扱う。

ワークフローは Marvelous Designer と同じ段取りで、**2 つのフェーズに分かれている**:

```
型紙を描く → 配置 → 縫う → Dress（着せる）→ Adjust（整える） ─┐  着せ付け：時間軸の外
                                                             ↓
                              着せた姿勢をフレーム1の出発点にする → 動かす → 出力
                                                                 アニメーション：時間軸に沿う
```

この分離が M7 の中心的な成果である。**縫い目が閉じていく数十フレームが
アニメーションの冒頭に入らなくなった。**

### 現時点でできること

| 領域 | 内容 |
|---|---|
| ソルバ | XPBD。伸び・曲げ（平均曲率の一次形式）・シーム・ゴム紐・つまみ・ピン留めの制約 |
| 質量 | 面積 × 密度による分布（一様質量ではない） |
| 力 | 重力、減衰、風（面圧として実装。重い生地ほどなびきにくい） |
| 品質プリセット | Draft / Normal / High / Final（+ Custom）。`bench_quality.py` の実測で決めた段 |
| 生地 | 7 プリセット（Chiffon〜Leather）。密度は実際の目付 g/m² 基準。サムネイル選択 |
| 衝突 | メッシュ衝突（BVH）、自己衝突（空間ハッシュ + 頂点-三角形）、床、摩擦、Catch Fast Motion（掃過判定） |
| コライダー | モディファイア/アーマチュア適用後の評価済みメッシュに追従。アニメーション対応 |
| 型紙 | **頂点属性 `muslin_pattern` として布の寸法の基準を別に持つ**。`Lock Pattern` / `Restore Pattern` |
| 縫製 | 型紙生成、輪郭からの生成、エッジ選択によるシーム定義、弧長ベース対応付け（頂点数が違う辺同士も縫える）、縫い線描画、`Validate Seams` |
| 着せ付け | `Dress`（時間軸の外で回し、落ち着いたら自動終了）、`Adjust`（Enter まで終わらない整えモード）、**左ドラッグで布をつまむ** |
| ゴム紐 | `Add Elastic From Selection`。辺属性 `muslin_elastic`、目標長を「型紙の長さ × 倍率」に。走らせたまま倍率変更可 |
| 重ね着 | 同じ `Group` 名の布を**1 つのシミュレーションにまとめて解く**。布どうしが双方向に押し合う。`Layer` で内側を優先 |
| 型紙連動 | `Create Pattern Object` で型紙を別オブジェクトに。**編集すると走っている布の寸法がその場で追従**（再生中 / Dress / Adjust） |
| 出力 | `Pattern to UV`（型紙の実寸比を保つ UV）、ディスクへのベイク、シェイプキー変換 |
| 並列化 | 4,000 頂点以上で制約・衝突を並列求解。**並列でも逐次でも結果は 1 ビットも変わらない** |

**設定はオブジェクト単位**（`Object.muslin`）。1 シーンでシルクとデニムを同居させられる。
UI は N パネルの Muslin タブに Solver / Advanced / Fabric / Forces / Collision /
Pinning / Pattern / Sewing / Elastic / Bake / Debug の各サブパネル。

---

## 2. 技術スタック

| 層 | 使っているもの | 役割 |
|---|---|---|
| 物理コア | **Rust 2021** (`rust/cloth_core`, 約 6,700 行) | XPBD ソルバ、BVH、空間ハッシュ、制約の色分け |
| 並列化 | **rayon** | 制約のブロック色分け並列求解、コライダー衝突・自己衝突 |
| Python 束縛 | **PyO3 0.22** (`abi3-py311`, `extension-module`) | Blender 同梱 Python から呼ぶ |
| ビルド | **maturin** → wheel → `.pyd` | Blender は `python.exe` を同梱しないので `maturin develop` は不採用 |
| アドオン層 | **Python / bpy** (`addon/muslin`, 約 6,700 行) | UI、モーダル操作、状態管理、座標受け渡し、ベイク |
| 配列処理 | **numpy**（Blender 同梱） | ローカル↔ワールド変換。素の Python ループから 10ms → 2.8ms に短縮 |
| 描画 | **gpu モジュール** (`overlay.py`) | 縫い線、型紙オブジェクトの外周・縫い目 |
| 永続化 | **Blender のメッシュ属性** | 型紙は頂点属性、縫い目・ゴム紐は辺属性。統合・細分化で一緒に運ばれる |
| 配布 | **Blender Extensions**（4.2 以降、`blender_manifest.toml`）+ 従来 zip | ネイティブモジュールを wheel として宣言 |
| CI | **GitHub Actions**（ubuntu / windows / macos） | `cargo test` と wheel ビルド + `verify_core.py` |

### 設計上の要になっている判断

- **物理コアを pyo3 非依存に保つ。** `crate-type = ["cdylib", "rlib"]` と
  `--no-default-features` により、**Blender も Python も無しで物理を検証できる**。
  CI が 3 OS で回せるのはこの構造のおかげ。
- **寸法の基準と初期配置を分ける。** 以前は組み立て時のワールド座標ひとつを
  「寸法」「初期配置」「着せた後の出発点」に兼用していて、**着せ直すたびに服が
  0.3% ずつ大きくなっていた**（6 回で +1.8%）。型紙を頂点属性に分離し、
  辺長・質量・曲げの重み・縫い目の弧長は型紙から、位置は今の形から取るようにした。
  回帰テストで固定済み（以前のやり方に戻すと落ちる）。
- **縫い目とゴム紐を頂点番号から切り離す。** 辺の整数属性（`muslin_seam` =
  `uid * 2 + 側 + 1` / `muslin_elastic`）で持つので、Blender が統合・細分化・削除に
  合わせて辺と一緒に運ぶ。**統合の後にピースを足しても細分化しても縫い目が消えない。**
  旧形式（頂点番号）も統合の前に属性へ移して引き継ぐ。
- **重ね着は 1 つのシミュレーションにまとめる。** 布ごとに頂点番号をずらして連結し、
  布どうしの衝突を自己衝突として双方向に効かせる。頂点ごとに生地（`set_materials`）と
  層（`set_layers`）を持つ。**先行事例（Taremin Cloth）が取れていない領域。**
- **並列化はブロック単位の色分けで。** 制約 1 本ずつ貪欲に色分けすると Gauss-Seidel の
  「端から順に伝わる」性質を失って Jacobi に近づき、**収束が落ちた**（伸び誤差
  0.139 → 0.228）。元の順番のままブロック（1024）に区切り、ブロック内は逐次にした。
- **abi3-py311 でビルドする。** Blender の同梱 Python が 3.13 に上がっても再ビルド不要。
- **wheel として配布する。** Windows がロード中の DLL を消せない問題を Extensions 形式で回避。
- **型紙は平面メッシュ／頂点属性で表現する**（当初案の UV はやめた）。Blender の
  編集ツールがそのまま使える。
- **bpy 非依存に切り出せるロジックは切り出す**（`seams.py` / `cache_io.py` /
  `transform.py` / `pattern_uv.py`）。テストが速く回る。

### 構成

```
rust/cloth_core/src/
  sim.rs         ソルバ本体（XPBD、各制約、風、生地・層、参照形状の差し替え）
  collision.rs   BVH・自己衝突・掃過判定
  bending.rs     曲げ制約（平均曲率の一次形式。静止形状を平面に投影して重みを作る）
  coloring.rs    制約のブロック色分け（並列求解の下地、GPU への前段）
  hashing.rs     GridHasher（SipHash を捨てた自前ハッシュ）
  math.rs        Vec3
  lib.rs         PyO3 束縛（ClothSim クラス + 補助関数）

addon/muslin/
  operators.py sim_state.py bake_ops.py sewing_ops.py pin_ops.py
  dress.py        Dress / Adjust のモーダル（時間軸の外で回す）
  grab.py         布をつまむ操作
  pattern_link.py 型紙オブジェクトと布の連動（編集のポーリング）
  pattern_uv.py   型紙から UV（bpy 非依存）
  properties.py panels.py previews.py overlay.py ui_poll.py
  mesh_io.py transform.py cache_io.py seams.py rest_shape.py
```

---

## 3. 検証体制

Blender を必要とする範囲を最小化した 3 層構造:

| 層 | 実行するもの | 項目数 | CI |
|---|---|---|---|
| 1. 物理コア | `cargo test --no-default-features` | 76 | ○（3 OS） |
| 2. 拡張モジュール + bpy 非依存部 | `scripts/verify_core.py` | 115 | ○（3 OS） |
| 3. アドオン層 | `blender --background --python scripts/blender_selftest.py` | 349 | ×（ローカル） |

品質の実測には `scripts/bench_quality.py`（品質段ごとの ms/frame・伸び誤差・垂れ）を使う。

自由落下と解析解の一致、球の貫通、縫い目の閉じ、**着せ直しで寸法が変わらないこと**、
**並列と逐次の結果がビット単位で一致すること**、ベイクした `.blend` の再読み込み、
入力バリデーションまで自動で見る。
**自動化できていないのは、ビューポート描画とパネルの見た目・操作の感触だけ**
（[CHECKPOINTS.md](CHECKPOINTS.md) に残件表あり）。

`samples/` の 5 シーン（ドレープ・旗・縫製・型紙から UV・型紙連動）はスクリプト生成で、
ヘッドレステストが毎回「開いて再生すると動く」ことを確認している。

---

## 4. 性能の現状

4,000 頂点以上では制約もコリジョンも並列。`iterations 10 / substeps 4`、
吊るした格子、min-of-7、20 スレッド（2026-09-24、Rust コア内部の時間）:

| 頂点数 | 制約のみ | + 自己衝突 | 並列化前（制約のみ / あり） |
|---|---|---|---|
| 1,681 | 4.8 ms | 8.3 ms | — |
| 4,225 | 7.4 ms | 11.9 ms | — |
| 14,641 | 8.0 ms | 21.3 ms | 37.5 / 52.8 ms |
| 29,929 | 13.9 ms | 42.8 ms | 77.5 / 113.5 ms |

M9 では約 3 万頂点・球に被せた場面・Normal 相当でも測っており、
自己衝突なし 209 → 70ms、あり 326 → 146ms。
**「3 万頂点の服を Normal で 1 フレーム 100ms 以下」は自己衝突なしなら達成**、
自己衝突ありは 146ms で残りの大半が自己衝突。

並列化で制約の割合が下がったため、**現在のボトルネックは自己衝突**
（とくに三角形の空間ハッシュの再構築）に戻っている。M9 の途中で見つけた
最大の当たりは「キャッシュした候補でのコライダー衝突が逐次のままだった」点で、
並列化して 122.8ms → 18.7ms になった。

効いたもの／効かなかったものは実測値ごと ROADMAP に残してある
（三角形ハッシュ再構築の並列化は 36.5 → 24.4ms になったが全体では
ばらつきの範囲だったので入れていない、など）。

---

## 5. 既知の制約

- 完全な CCD は無い。コライダー相手（60 m/s）と布どうし（掃過判定で 12 m/s まで）は
  塞いであるが、**布どうしが互いに高速ですれ違う**場合は取りこぼす。
  また Catch Fast Motion は頂点だけを戻す作りなので、**重ね着の層を見ていない**
- 自己衝突はエッジ同士の交差を見ていない（実測で違反の 0.1%）
- 摩擦は速度減衰の簡易モデルで、物理的な μ ではない
- **曲げ剛性が solver の反復数に依存する**（PBD 系共通の性質）。Substeps が低いと
  生地プリセットの差がほぼ出ない。Normal（substeps 8 + Chebyshev 0.98）以上を使う
- `Self Thickness` の上限はメッシュのエッジ長で決まる（細分化は逆効果）
- **重ね着で外側の重さは内側に伝わらない**（服を体へ押し込まないことを優先した割り切り）
- 型紙連動は**トポロジが変わらない編集のみ**（頂点数を変える編集は不可）
- 別オブジェクトのパターン同士は縫えない（統合してから縫い目を定義する。
  ただし統合後にピースを足す・細分化するのは可）
- 縫い目が閉じる速さが組み立て時の配置距離に依存する（離して置いたピースほど速く閉じる）
- ベースメッシュの頂点を直接書き換えるため、モディファイアスタックとの相性に制約がある
- アニメーション開始時にアバターの初期ポーズと着せた時のポーズがずれていても警告が出ない
- 実機（Blender に入れての）確認は Windows のみ

---

## 6. 今後の展望

### 使い勝手の判断待ち（M8 の未決）

いずれも実装の問題ではなく「どちらが自然か」の設計判断:

- 型紙を伸ばすと柄（UV）も一緒に引き伸ばされる。このままか、**生地が足された
  ように柄は実寸のまま広がるべきか**（型紙が変わるたびに UV を作り直す）
- 型紙オブジェクトのピースが布と同じ配置なので前身頃と後ろ身頃が重なって見える。
  Marvelous Designer のように横に並べるか
- 型紙オブジェクトの線の太さ・色、編集中の追従の見え方

### 品質面の宿題

- **曲げ剛性の反復数依存を断つ。** 現状いちばん体験を損ねている。品質プリセットで
  実用的な段は用意できたが、根本は残っている。広い間隔の曲げ制約を足す案は
  **計測して退けた**（硬さは出るが球に巻き付かず板になる）。別の定式化を検討する余地
- **縫い目が閉じる速さを配置距離から切り離す**
- アバターのポーズずれの警告、着せた状態からのスクラブ・決定性の再確認
- エッジ-エッジ自己衝突、物理的な摩擦係数

### 速さ — M4: GPU 化（未着手）

**着手条件を「M9 の色分けで足りないと実測で分かってから」と決めている。**
M9 で CPU の並列化が効いたので、当初想定していた緊急性は下がった。進めるなら
M9 のブロック色分けをそのまま wgpu に載せるのが筋（色分けは GPU の前段として使える）。

留意点として ROADMAP に残してあるのは:

- GPU で atomic に足すと加算順序が揺れ、**ベイクとスクラブの決定性が崩れる**
- 硬い生地が硬くならないのは収束の問題なので、1 反復を速くしても直らない
  （Jacobi にすると悪化する）

残る最大のコストは自己衝突なので、GPU 化の狙いは「制約」より
**自己衝突と三角形ハッシュ**に移る可能性がある。

### 機能面の候補

- **トポロジが変わる型紙編集**（型紙の 2D 位置で着せた形を移し替える）
- **2D の型紙編集画面**、cm での寸法入力、アバターを囲む曲面への配置
- 別オブジェクト間の縫製
- 厚みを付けた出力（Solidify 相当）、四角面に戻す処理
- マテリアル単位の物性、Blender 標準の Force Field 連携
- ゴム紐の剛性倍率（現状は縮める倍率だけ。必要が見えてから）

### 運用面

- **Windows 以外での実機確認**。物理コアは 3 OS で CI を通し、Release ワークフローが
  4 プラットフォームぶんの wheel を同梱した zip を作るところまで用意してあるが、
  Linux / macOS の Blender に入れての確認はまだ
- ビューポート描画とパネルの見た目の検証方法（現状は完全に手作業）
- Extensions Platform への公開
